version: '3.8'
services:
  postgres:
    image: postgres:15
    container_name: letsmeet_db
    environment:
      POSTGRES_USER: letsmeet_user
      POSTGRES_PASSWORD: letsmeet_password
      POSTGRES_DB: letsmeet
    ports:
      - "5432:5432"
    restart: unless-stopped
